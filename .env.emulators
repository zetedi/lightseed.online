# Loaded by `vite --mode emulators` (npm run devui) ON TOP of .env/.env.local.
# The one switch that binds the app to the local Firebase Emulator Suite: no internet,
# no production reads, no data spend. See services/firebase/core.ts.
VITE_USE_EMULATORS=true
